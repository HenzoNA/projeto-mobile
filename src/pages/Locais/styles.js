import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
    alignItems: "center",
  },
  flatList: {
    marginBottom: 10,
    width: "100%"
  },
});
